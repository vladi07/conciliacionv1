{% extends 'base.html.twig' %}

{% block title %}Documentacion index{% endblock %}

{% block body %}
    <h1>Documentacion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TipoDocumento</th>
                <th>Ruta</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for documentacion in documentacions %}
            <tr>
                <td>{{ documentacion.id }}</td>
                <td>{{ documentacion.tipoDocumento }}</td>
                <td>{{ documentacion.ruta }}</td>
                <td>
                    <a href="{{ path('documentacion_show', {'id': documentacion.id}) }}">show</a>
                    <a href="{{ path('documentacion_edit', {'id': documentacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('documentacion_new') }}">Create new</a>
{% endblock %}
