{% extends 'base.html.twig' %}

{% block title %}Documentacion{% endblock %}

{% block body %}
    <h1>Documentacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ documentacion.id }}</td>
            </tr>
            <tr>
                <th>TipoDocumento</th>
                <td>{{ documentacion.tipoDocumento }}</td>
            </tr>
            <tr>
                <th>Ruta</th>
                <td>{{ documentacion.ruta }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('documentacion_index') }}">back to list</a>

    <a href="{{ path('documentacion_edit', {'id': documentacion.id}) }}">edit</a>

    {{ include('documentacion/_delete_form.html.twig') }}
{% endblock %}
