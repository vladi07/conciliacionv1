<form method="post" action="{{ path('externo_delete', {'id': usuario_externo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario_externo.id) }}">
    <button class="btn">Delete</button>
</form>
