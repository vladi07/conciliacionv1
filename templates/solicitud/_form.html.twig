{{ form_start(formulario) }}
    {#
    Renderizamos en la vista el formulario enviado desde el SolicitudController
    La variable a renderizar es 'formulario'
    #}
    {{ form_widget(formulario) }}

    <button class="btn btn-success">
        <i class="bi bi-bookmark-check"></i>
        {{ button_label|default('Guardar') }}
    </button>
{{ form_end(formulario) }}
