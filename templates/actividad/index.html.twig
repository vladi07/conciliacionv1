{% extends 'base.html.twig' %}

{% block title %}Actividad index{% endblock %}

{% block body %}
    <h1>Actividad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Fecha</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for actividad in actividads %}
            <tr>
                <td>{{ actividad.id }}</td>
                <td>{{ actividad.descripcion }}</td>
                <td>{{ actividad.fecha ? actividad.fecha|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('actividad_show', {'id': actividad.id}) }}">show</a>
                    <a href="{{ path('actividad_edit', {'id': actividad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('actividad_new') }}">Create new</a>
{% endblock %}
