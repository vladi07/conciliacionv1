<form method="post"
      action="{{ path('eliminar_usuario',{'id':verUsuario.id}) }}"
      onsubmit="return confirm('¿Esta seguro de eliminar este USUARIO?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ verUsuario.id) }}">
    <button class="btn btn-danger">
        <i class="far fa-trash-alt"></i>
        Eliminar
    </button>
</form>